{% extends 'base.html.twig' %}

{% block body %}
    <div class="example-wrapper">
        {% if isedited == false %}
            <h1>Edit {% if agent.name is same as ('47') %} Agent 47 {% else %} agent {{ agent.name }}{% endif %}</h1>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input class="button" type="submit" value="edit agent"/>
            {{ form_end(form) }}
            <button onclick="window.location.href = '{{ path('app_agent_list') }}'">Back to agent list</button>
        {% else %}
            <h1>The
                agent {% if agent.name is same as ('47') %} Agent 47 {% else %} {{ agent.name }}, {{ agent.surname }} {% endif %}
                has been edited! ✅</h1>
            <button onclick="window.location.href = '{{ path('app_agent_list') }}'">Back to agent list</button>
        {% endif %}
    </div>
{% endblock %}
