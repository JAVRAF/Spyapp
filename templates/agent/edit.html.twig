{% extends 'base.html.twig' %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    {% if isedited == false %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="edit agent"/>
    <button onclick="window.location.href = 'http://localhost/agent/'">Back to agent list</button>
    {{ form_end(form) }}
    {% else %}
    <h1>The agent {% if agent.name is same as ('47') %} Agent 47 {% else %} {{ agent.name }}, {{ agent.surname }} {% endif %} has been edited! ✅</h1>
        <button onclick="window.location.href = 'http://localhost/agent/'">Back to agent list</button>
    {% endif %}
</div>
{% endblock %}
