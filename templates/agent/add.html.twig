{% extends 'base.html.twig' %}


{% block body %}
    <div class="example-wrapper">
        {% if isadded == false %}
            <h1>Add a new operative :</h1>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input class="button" type="submit"/>
            {{ form_end(form) }}
            <button onclick="window.location.href = '{{ path('app_agent_list') }}'">Back to agent list</button>
        {% else %}
            <h1>The
                agent {% if agent.name is same as ('47') %} Agent 47 {% else %} {{ agent.name }}, {{ agent.surname }} {% endif %}
                has been added! ✅</h1>
            <button onclick="window.location.href = '{{ path('app_agent_add') }}'">Add another agent</button>
            <button onclick="window.location.href = '{{ path('app_agent_list') }}'">Back to agent list</button>
        {% endif %}
    </div>
{% endblock %}
